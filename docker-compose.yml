version: '3.8'

services:
  anima:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "runtime/cli.js", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
